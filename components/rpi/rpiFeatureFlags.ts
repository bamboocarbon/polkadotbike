// Temporarily hides all affiliate/ad content on Rebecca's Private Idaho
// pages (Performance Bicycle BuyCard, BikesBookingCard, KiwiCard, the
// Stay22 embed, and the AADSUnit ad banner) while Robin emails the event
// organisers to check whether they want/will allow the site to be
// promoted before the event (2026-08-31). Flip back to true once that's
// resolved — every block on the index page and the individual route
// pages already reads this flag, so nothing else needs to change.
export const RPI_AFFILIATES_ENABLED = false;
