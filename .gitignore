.vercel
.DS_Store
*.bak
*.log
nohup.out

# Next.js
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# Phase 0.4 migration baseline — local reference screenshots (~25MB), not
# meant to live in git history. Kept on disk for the Phase 5 visual diff.
baseline/
.env*.local

# npm run calendar:export output
dist/

# Climb basemap/terrain build output — uploaded to R2 (assets.polkadotbike.com)
# by scripts/build-climb-basemaps.ts and build-climb-terrain.ts, not shipped
# in the repo/public/. See project_cyclegear memory, Sept 2026.
.basemap-staging/
